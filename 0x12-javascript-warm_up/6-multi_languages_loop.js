#!/usr/bin/node
// Print strings using an array and a loop

const myArray = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let i = 0;

while (i < myArray.length) {
  console.log(myArray[i]);
  i++;
}
